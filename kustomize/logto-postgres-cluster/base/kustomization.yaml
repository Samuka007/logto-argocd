apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base 只包含非敏感配置
# SealedSecret 在各个 overlay 中定义（dev/prod 使用不同密码）
# backup-schedule 在 prod overlay 中启用
resources:
- cluster.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/component: database
    app.kubernetes.io/name: logto-postgres
