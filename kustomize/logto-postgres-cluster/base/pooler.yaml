apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: logto-postgres-pooler
spec:
  cluster:
    name: logto-postgres
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "100"  # 调整为 logto 所需
      default_pool_size: "20"
