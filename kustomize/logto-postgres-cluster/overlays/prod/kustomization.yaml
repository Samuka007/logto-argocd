apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

# CNPG 自动生成 logto-postgres-app Secret，无需手动创建数据库凭据
resources:
- ../../base
- sealed-secret-backup.yaml  # 仅备份凭据（S3）
- backup-schedule.yaml

patches:
- path: patches/cluster-resources.yaml
